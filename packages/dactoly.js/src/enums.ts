export enum ChannelType {
  DM = 'DM',
  GROUP = 'GROUP',
}

export enum FriendRequestStatus {
  ACCEPTED = 'ACCEPTED',
  BLOCKED = 'BLOCKED',
  PENDING = 'PENDING',
  REJECTED = 'REJECTED',
}

export enum MessageType {
  DEFAULT = 'DEFAULT',
  SYSTEM = 'SYSTEM',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
